{% extends 'base.twig' %}
{% block body %}
    <div class="container">
        <img src="https://picsum.photos/150/75" class="card-img-top img-fluid "  style=" width: 100%; height: 250px;
             object-fit: cover;"  alt="...">
        <h1 class="mt-3">{{ post.title|escape }}</h1>
        <div class="container mb-3">
            <div class="row">
                <span class="text-muted"> Marianov Yevhen | {{ post.published_date|daty ("F d, Y") }}24 </span>
            </div>
            <div class="mt-3">{{ post.content|raw }}</div>
        </div>
    </div>
{% endblock %}